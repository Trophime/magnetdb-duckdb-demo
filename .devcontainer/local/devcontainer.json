{
  "name": "M1S2 Hifimagnet projects (local mount)",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": "..",
    "args": {
      "BASE_IMAGE": "trophime/magnettools:trixie"
    }
  },

  "runArgs": [
    "--cap-add=SYS_PTRACE",
    "--security-opt",
    "seccomp=unconfined",
    "--net",
    "host",
    "-e",
    "DISPLAY=${env:DISPLAY}",
    "-v",
    "/tmp/.X11-unix:/tmp/.X11-unix",
    "-v",
    "${env:HOME}/.Xauthority:/home/myuser/.Xauthority"
  ],

  "mounts": [
    "source=${localEnv:HOME}/LNCMIG-Data/records,target=/mnt/LNCMIG-Data/records,type=bind,consistency=cached",
    "source=${localEnv:HOME}/github/hifimagnet-projects,target=/home/myuser/hifimagnet-projects,type=bind,consistency=cached"
  ],

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python"
      }
    }
  },

  "postCreateCommand": "chmod +x .devcontainer/setup-basic.sh && ./.devcontainer/setup-basic.sh",
  "postStartCommand": "[ -d .venv ] || (chmod +x .devcontainer/setup-basic.sh && ./.devcontainer/setup-basic.sh)",

  // Add a remote user (see Dockerfile) 
  // and use it as the default user. On Linux, the container user's GID/UIDs
  // will be updated to match your local UID/GID (when using the dockerFile property).
  // See https://aka.ms/vscode-remote/containers/non-root-user for details.
  // The default user in the base image is "jovyan", so we use that here. 
  // If you change the user in the Dockerfile, make sure to update this value.
  "remoteUser": "myuser",

  "forwardPorts": [8866],
  "portsAttributes": {
    "8866": { "label": "Voila App" }
  }
}
